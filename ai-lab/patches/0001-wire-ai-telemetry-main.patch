diff --git a/source/main.cpp b/source/main.cpp
index b58be8266..d65894150 100644
--- a/source/main.cpp
+++ b/source/main.cpp
@@ -18,0 +19 @@ this program. If not, see <https://www.gnu.org/licenses/>.
+#include "AiHooks.h"
@@ -57,0 +59,2 @@ this program. If not, see <https://www.gnu.org/licenses/>.
+#include <cstdint>
+#include <cstdlib>
@@ -110,0 +114 @@ int main(int argc, char *argv[])
+	AiHooks::Options aiHookOptions;
@@ -148,0 +153,8 @@ int main(int argc, char *argv[])
+		else if(arg == "--ai-telemetry")
+			aiHookOptions.telemetry = true;
+		else if(arg == "--ai-telemetry-every" && *++it)
+		{
+			aiHookOptions.telemetryEvery = atoi(*it);
+			if(aiHookOptions.telemetryEvery < 1)
+				aiHookOptions.telemetryEvery = 1;
+		}
@@ -149,0 +162 @@ int main(int argc, char *argv[])
+	AiHooks::Configure(aiHookOptions);
@@ -402,0 +416 @@ void GameLoop(PlayerInfo &player, TaskQueue &queue, const Conversation &conversa
+		uint64_t aiHookTick = 0;
@@ -437,0 +452,3 @@ void GameLoop(PlayerInfo &player, TaskQueue &queue, const Conversation &conversa
+			if(AiHooks::TelemetryEnabled())
+				AiHooks::EmitTelemetry(player, ++aiHookTick);
+
@@ -627,0 +645,2 @@ void PrintHelp()
+	cerr << "    --ai-telemetry: print AI Lab telemetry as JSON Lines to stdout." << endl;
+	cerr << "    --ai-telemetry-every <frames>: telemetry frame interval, default 60." << endl;
